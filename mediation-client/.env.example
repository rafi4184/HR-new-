# Same Supabase project as client/.env.example — this app reads the
# mediation-platform tables from that project via RLS, not the concierge
# site's RPCs.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
